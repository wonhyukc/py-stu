# To-Do List (2todo.md)

## [x] 해결해야 할 이슈 (Pending Issues)

- **에러 사항**: 터미널에서 스크립트 실행 후 브라우저 OAuth 로그인 시 `Error 403: access_denied` 현상 발생.
- **원인**: 현재 연결된 `credentials.json` (프로젝트명: `calendar_ntnm`)의 OAuth 동의 화면 상태가 '테스트 중(Testing)' 이며, 로그인하려는 계정(`wonhyukc@stu.ac.kr`)이 테스트 사용자 명단에 포함되어 있지 않음.
- **해결 방안 선택 (TODO)**:
  1. **구글 클라우드 콘솔 접속 (추천)**: 해당 클라우드 콘솔(GCP)에 들어가서 `[API 및 서비스] -> [OAuth 동의 화면]` 메뉴 하단의 **[테스트 사용자 (Test users)]** 목록에 `wonhyukc@stu.ac.kr` 이메일을 직접 추가.
  2. **다른 키 사용**: 콘솔 접속이 귀찮거나 번거롭다면, 정상 동작하는 프로덕션 레벨이거나 알맞게 설정된 다른 프로젝트의 OAuth 키(`.ssh/` 폴더 내 다른 400바이트 짜리 json 파일)로 심볼릭 링크를 재지정(`ln -s`).

## [ ] 다음 작업 목표
- 위 오류 해결 후 다시 한 번 스크립트를 실행(`bin/check_assignments.py`)하여 0.4 과제 채점이 정상적으로 터미널에 출력되는지 확인.
- 정상 출력 확인 시, 채점 결과를 바탕으로 구글 시트에 점수를 자동으로 기입하는 추가 기능 구현.
